# BINI Blooms PMS — 版本更新歷史

---

## Unreleased — Firebase v4 全介面／全功能雲端搬移（DEV）

- 建立 BINI Design System v1：將既有粉色品牌演進為 primitive／semantic 雙層 Tokens，涵蓋色彩、字級、4px 間距、圓角、陰影、motion、44px touch target 與房態語意；新增 Button、Badge、SectionCard、Field、Notice、ResponsiveDialog 六個 React 核心元件與 contract tests，並開始套用到登入、MFA、今日房態、帳號管理及首次資料導入。
- 補上手機版遺漏的「中／EN」語言切換，並啟用原先停用的桌機語言按鈕；兩種版面及登入流程共用 `LocaleProvider`，切換不需重新載入、會記住選擇，且目前已完成的導覽、房態、操作、帳號與匯入文字同步切換。
- 明確鎖定 v4 為 **v3.9.14 全介面、全功能雲端搬移**：桌機版保留現有頂部導覽、頁面與操作流程，只有手機版依小螢幕改成卡片／全螢幕表單／bottom sheet；新增 `docs/cloud/v3-v4-full-parity-matrix.md`，逐頁追蹤 22 個介面／替代項目。實作順序僅為風險切片，不代表縮減範圍。
- 啟動全資料移轉契約：新增 12 個權威資料表到 Firestore collection 的明確對照、穩定 legacy document ID、空館別映射 `property-main`、Asia/Taipei 時間轉換、整數新台幣與 SHA-256 來源中繼資料；密碼雜湊、備份憑證、`Room.next_booking` 與 `report_summary` 明確不可匯入為權威資料。
- 新增完整 17 分頁導覽 manifest，桌機版改回 v3 同順序的頂部導覽與三欄房卡，手機版保留五個主分頁並可由「更多」到達所有有權限的低頻功能；移除會誤導為真實狀態的固定「2 筆待同步」假資料，明確標示 DEV 尚不可營運。
- 「今日房態」採同一份 room view model 提供雙版面：桌機房卡直接顯示 v3 的旅客、入住／退房、款項、下一筆預約、備註與快捷操作；手機房卡維持精簡，點擊房號後以詳細面板顯示相同資料與操作。
- 「今日房態」已由展示陣列改為 property-scoped Firestore 即時投影：監聽 rooms、bookings、stays、payments、maintenanceSchedules，共用 v3 active-stay 款項範圍、七種房態與維修排程覆蓋；下一筆預約只取 `已預約` 且晚於目前時間的最早資料，並每分鐘重算，避免過期時間殘留。真實登入失敗時 fail closed，不回退假房間；快捷操作的 server handlers 仍待實作。
- 「預約管理」已改為 property-scoped Firestore 即時清單：保留 v3 僅顯示有效 `已預約`、依入住時間排序與房號／旅客／預約編號／電話搜尋的規則。真實登入的讀取失敗或資料格式不符時 fail closed，不會顯示展示預約。
- 「新增預約」已接入第一個正式 PMS 寫入閉環：`bookingCreate` 只允許完成 email 驗證與 TOTP MFA、且被管理員授權 `bookings_new` 頁面的帳號呼叫。它在同一筆 Firestore transaction 內驗證房間、同房有效預約、在住房、未完成維修排程與月租房限制，伺服器端重算 v3 台灣假日／12 小時區塊／多日／折扣計價，並可原子建立訂金付款、audit 與可重試 operation 紀錄；client 不能直接寫任何權威 collection。桌機提供完整欄位，手機以單欄觸控表單呈現。No-show、多時段與送出前 availability／quote 顯示仍待實作。
- 「即將入住」已接入 property-scoped Firestore 即時提醒：只顯示仍為 `已預約`、且嚴格位於未來 15 分鐘內的預約，並每 60 秒重新計算。桌機與手機共用同一份提醒資料；「保留預約」僅在目前瀏覽器工作階段隱藏，「標記 No-show」必須二次確認並重用 `bookingCancel` 的受 MFA／`bookings` 權限保護 transaction，將狀態改為 `已取消`、寫入 `booking.no_show` audit 與可重試 operation，不異動付款、stay 或房間。提示音尚未搬移。
- 上述提醒與 No-show 標記已發布至 DEV：既有 `bookingCancel` 已更新為 `ACTIVE`、Node.js 22／512 MiB；最新 Hosting bundle `index-Bdoo7KvG.js` 已上線。未執行任何實際資料寫入，營運寫入 smoke 仍須在管理員完成 MFA 並建立／匯入測試資料後進行。
- 「修改預約」已接入 `bookingUpdate`：仍要求 MFA 與 `bookings` 權限，會排除自身後重新檢查目標房間的有效預約、在住房與未完成維修，伺服器端依 v3 規則推導退房與自動／手動金額。transaction 只更新 booking、version、audit 與 operation；既有訂金付款不會被這個流程改動。預約明細可進入完整預填表單，桌機雙欄、手機單欄共用同一個 contract。
- 「入住登記」已接入 `stayCheckIn`：具 MFA 與 `checkin` 頁面權限的帳號可由有效預約帶入或建立 walk-in。它在一筆 Firestore transaction 內驗證可入住的房態、既有 stay、同房有效預約與維修時段，伺服器依 v3 假日／方案／天數規則重算金額，原子建立 stay、房間轉「使用中」、來源預約轉「已入住」、選填押金、audit 與可重試 operation。桌機／手機共用同一表單，房間總覽的「辦理入住」直接進入該流程；舊單機版覆蓋既有 stay 的行為不搬移。
- 「延住處理」已接入 `stayExtend`：具 MFA 與 `extend` 頁面權限的帳號可選取即時在住房，查看原／目前／新退房、目前與累計延住費、應收與逐區塊預覽。延住費完全沿用 v3 的入住時間軸與 12 小時封頂規則，因此 12 小時延至 24 小時只收該時段差額，不會重新計算為新的一段住宿。送出時會在同一 Firestore transaction 再次檢查房間、未來有效預約與未完成維修，若撞期即拒絕且不寫入任何資料；成功後原子更新 stay／room、audit 與可重試 operation。單機版「先寫入、僅顯示撞期警告」的行為刻意提升為雲端 fail-closed，避免跨裝置超賣。
- 「退房辦理」已接入 `stayCheckout`：具 MFA 與 `checkout` 頁面權限的帳號可選取在住房、填寫雜費與選填逾時費調整，並以二次確認送出。伺服器以自身時間計算 15 分鐘免費取消、15 分鐘退房緩衝、半小時進位的逾時費，免費取消時建立符合 stay scope 的押金退款；同一 transaction 建立 stay log／退款／audit、房間轉待清潔並刪除 active stay。人工逾時調整保留系統與實收金額供 audit 比對。
- 「付款管理」已接入一般收款、追加式退款與手動例外收款：即時讀取付款紀錄與在住房，提供 v3 同等的當日實收／退款／淨額／待收與付款方式摘要。`paymentCreate` 需 MFA 與 `payments` 頁面權限，會在單一 transaction 驗證 stay／room identity 與可收款房態，原子建立 payment、audit 與可重試 operation；client 不可直接寫帳務。`paymentRefund` 同樣受 MFA／`payments` 保護，原付款永不覆寫或刪除，而是建立關聯的退款 payment；伺服器 transaction 重新檢查原付款、既有退款累計與可退餘額。`paymentManualCreate` 則供無法使用在住房收款的例外，強制旅客、原因與正數金額；房號選填但填寫時必須存在，若未來 API 關聯預約也會重查同館別與房間一致性。三者皆寫入 audit／可重試 operation。訂金調整、刪除、日結與 CSV 匯出仍待獨立 transaction／報表切片完成。
- 「清潔管理」已接入 `housekeepingUpdate`：即時列出待清潔與清潔中的房間，並只接受 v3 的待清潔 → 清潔中 → 可入住單向流程。MFA 與 `housekeeping` 頁面權限在 transaction 內重新驗證，成功後原子更新房態、version、audit 與可重試 operation；手機與桌機共用觸控友善的房務卡片。
- 「維修管理」已接入排程閉環：即時列出維修排程，可建立房號、標題、起訖時間與備註，亦可標記完成或刪除排程。`maintenanceScheduleCreate` 與 `maintenanceScheduleAction` 均要求 MFA／`maintenance` 權限；建立時重新檢查房間與同房有效預約，完成／刪除時以同一筆 transaction 寫入 schedule、audit 與可重試 operation。維修房解除、進度備註與篩選仍待後續切片。
- 已確認 DEV 的 `properties/property-main/rooms` 為 0 筆，故所有依房間集合建立的選取控制項會停用或無選項；這不是前端假資料問題。房態空白時管理員現在可直接開啟一次性 Dropbox 初始資料導入，必須以原單機版 `bini_blooms_backup.json` 完成對帳與確認 promotion 才會安全建立原有六間房。
- 新增 admin/MFA 限定「初始資料導入」：接受單機版 Dropbox `bini_blooms_backup.json`（schema 3.5），本機預覽 12 表筆數並先移除舊使用者／密碼、彙總報表、未知欄位、備份設定及 `rooms.next_booking`，再由 `adminStageV3Backup` 驗證 SHA-256 並以穩定 ID 寫入 default-deny staging；新增 `adminPrepareV3Backup` 將 12 類資料轉為 property-scoped typed documents，檢查日期、整數 NTS、狀態、外鍵與重複 active stay，並回傳逐表對帳報告。
- 新增一次性 DEV 正式匯入 promotion：對帳通過後，管理員必須輸入該批次專屬確認字串，`adminPromotePreparedV3Backup` 才會重新檢查館別、checksum、轉換版本、逐表筆數、prepared 文件路徑與 migration metadata。除已存在的雲端館別根設定外，系統只會建立不存在的文件；根設定會保留 `name`／`active`／`currency`／`timezone`，並一次性附加 legacy property 資料，其餘既有資料一律拒絕覆寫。中斷後只能以相同批次、完全相同的文件安全續作，並與成功狀態原子寫入批次／audit 記錄。此版本僅提供程式與 DEV 部署能力，尚未對任何真實 Dropbox 備份執行 promotion，匯入後的復原演練仍待完成。
- 修正真實 v3 備份的匯入相容性：月租續租歷史 `renewed`、免費取消形成的同時刻或早於原訂入住的 stay log，以及小數展示時薪不再錯誤阻擋整批對帳。提前取消會保留原訂入住時間並正規化為零時長歷史紀錄；小數時薪依 v4 既有規則向下取整，權威住宿總額保持原值。轉換版本已提升為 3，使舊版 `blocked` 批次可安全重新對帳；尚未進行正式 promotion。
- 初始資料導入頁改為顯示 callable 的具體失敗原因，避免將前一次 promotion 的失敗訊息誤判為目前備份格式錯誤；管理員可據同一批次狀態繼續對帳或 promotion。
- 已完成真實 Dropbox v3 備份批次 `93ba8b3ce620…` 的 Firebase DEV promotion：1,584 筆來源與 prepared 資料完成對帳並 create-only 寫入。驗收確認六間房 `201、202、203、205、206、207`、97 筆預約、3 筆在住房、140 筆歷史住宿、93 筆付款、34 筆月租與 241 筆假日均已存在；auditLogs 為 971 筆，包含此次 migration audit。
- 「房間管理」已接入真實 Firestore 即時讀取與受保護操作：具 MFA＋`room_management` 權限的使用者可修改一般備註與維修房態（維修說明／預計完成日必填）、建立月租、續租一個月及月租退房。每個操作皆以 UUID 冪等交易更新 room／monthlyRental／必要付款與 append-only audit；月租建立收取租金與押金、續租僅收新一期租金、退租退款不得超過押金且房間會轉為待清潔。桌機直接顯示完整月租／維修資訊；手機點擊房卡後以明細 sheet 操作。入住中的房態仍只允許由入住／退房流程變更。
- 新增「換房」雲端交易：房間管理即時讀取在住房資料；桌機可直接換房，手機由房卡詳細 sheet 開啟換房表單。`stayTransfer` 要求 MFA＋`room_management` 權限，於單一 transaction 重新檢查來源房態、目標可入住狀態、有效預約及未完成維修排程；成功後同步轉移 active stay／已入住來源預約、將原房設為待清潔，並建立零金額轉房 stay log、audit 與可重試 operation。未關聯的未來預約與付款歷史不會被搬移。
- 「房間總覽／今日營運」補上七種房態與全部房間的即時篩選磚，顯示每種房態的房數；篩選只改變畫面投影，不修改 Firestore 即時來源。桌機持續直接呈現旅客、入住／退房、款項、下一筆預約與快捷操作；手機保持點擊房卡後再開啟完整詳細資料與操作 sheet。
- 新增 Firebase 即時「房間甘特圖」：以台灣時區的未來 14 天為視窗，投影房間、有效預約、在住房、未完成維修與 active 月租；過期／取消項目不顯示。桌機提供固定房號欄與可橫向捲動時間軸，手機同樣保留橫向時間列；點選任何色塊可查看類型、項目與起迄時間。
- Prototype Hub 已改為依目前登入帳號的 `allowedPages` 顯示可用模組卡片；所有入口仍交由既有權限與頁面 router 處理，管理員才額外看到初始資料導入，不會藉由 Hub 繞過分頁權限。
- 將程式品牌改為專案既有 BINI Blooms 橫式 logo，移除臨時機器人 SVG；使用專案房屋圖檔建立真正透明的 192／512 PWA icon、獨立 maskable icon、Apple touch icon 與 favicon，並更新 manifest 及 Service Worker cache，供手機「加入主畫面」顯示正確 App 圖示。
- 以 2026-09-09 單機備份副本完成唯讀 dry-run：579,199 bytes／1,589 筆權威候選資料可解析，清理後為 431,817 bytes；5 筆舊使用者已排除，清理後內容不含 password hash/salt。此檔僅作相容性驗證，不作最終搬家來源。

- 建立獨立 `cloud/` TypeScript monorepo：共享 operation contract、Cloud Functions processor、Firestore Rules 與 React/Vite PWA；不改寫 v3 SQLite 或桌面版 3.9.14。
- 完成單機版唯讀盤點與移轉決策：保留角色、分頁權限、館別、假日／計價及 PMS domain；不移轉本機 password hash、session key、`backup_state`／`backup_logs`／`backup_config` 或任何 Dropbox/WebDAV/FTP/Google Drive 憑證。
- 網路版以 Firestore 作為即時權威資料源，手機「更多」與設定頁不提供單機版日常雲端備份、自動備份或還原入口；只保留一次性初始資料導入，資料備援另採 Firebase 匯出／災難復原流程。
- 完成封閉式員工 Auth：沒有公開註冊，管理員可新增、啟停、設定角色／17 個分頁權限與重設密碼；密碼只寫入 Firebase Auth，不進 Firestore 或 audit log。
- 強制 email 驗證與 TOTP MFA；未完成 MFA、停權、跨館別與非 admin 的帳號管理請求均拒絕。首位 DEV admin `biniblooms250808@gmail.com` 已由 server-side bootstrap 建立並寄出一次性密碼設定信。
- 所有權威資料維持 server-only write；客戶端只能建立 append-only operation request，處理器具 idempotency、版本衝突偵測與 audit log。
- 手機版採專用資訊架構：底部五分頁、快捷操作、房態卡與 bottom sheet；使用 44px 觸控目標、safe-area、房態語意色及窄螢幕無水平溢位，桌面寬度切換為 v3 頂部導覽。
- DEV Firestore 已建立於 `asia-east1`；Rules、indexes、十七個 Node.js 22 Functions 與 Hosting 已部署至 `bini-transient-dev`，PROD `bini-transient` 未部署、未修改。
- 修正 Hosting 空白頁：workspace Vite 明確由 `cloud/.env.local` 讀取 DEV 設定；新增 bundle guard，缺設定、placeholder 或非 DEV project 時禁止部署。
- DEV 預覽：`https://bini-transient-dev.web.app`。實際手機瀏覽器確認登入卡正常、無目前版本 console error，且頁面沒有註冊或外部備份入口。
- 驗證：145 項 Vitest、6 項 DEV 部署防護、typecheck、lint 與 production build 通過；Rules Emulator 最近完整結果為 41/41（本次未改 Rules）。DEV live function list 確認十五個 Functions 全位於 `asia-east1`，其中新增的 `paymentCreate` 為 Node.js 22／512 MiB、ACTIVE。首頁與最新線上 bundle `index-DfdlN6xD.js` 均 HTTP 200，bundle 已包含付款頁與 `paymentCreate`。瀏覽器版面契約涵蓋 320／375／430／768／1100px；未登入 UI preview 未進 production build。
- 限制：目前已完成單筆預約建立／修改／取消、15 分鐘 No-show 視覺提醒／人工標記、入住、延住、退房、在住房一般收款、追加式退款與手動例外收款；首次資料 promotion 僅為一次性 migration callable。提醒提示音、多時段、訂金調整／刪除／日結／CSV、房務、維修、報表等尚未接入正式 domain handlers，不可作為正式營運版。

---

## v3.9.14 `2026-09-06` — 超時費減免審計軌跡；備份改為「資料變動即存」

### 1. 超時費減免會留下審計軌跡
- 背景:退房時服務人員可把超時費調低甚至改成 0（忘按退房、客人非上班時間提早離開等，屬合法操作）。但**過去這個調整完全沒有留痕跡**，導致「合法減免」與「誤操作/漏收」無法區分。
- 修正:退房時若**實收超時費低於系統計算值（含改成 0）**，自動寫入一筆 **「超時費減免」審計軌跡**，記錄:房號、旅客、**系統應收**、**實際收取**、**減免金額**、操作人、時間。
- 全額照收（未調整）或免費取消則**不會**產生此紀錄。
- 審計軌跡頁新增可篩選的「超時費減免 🟡」類別。
- 實測:改 0 → 記錄「系統應收 1300 → 實收 0（減免 1300）」；改 500 → 記錄「1300 → 500（減免 800）」；全額自動收 → 不記錄。

### 2. 取消 30 分鐘定時備份，改為「資料一變動就自動儲存」
- 移除每 30 分鐘一次的定時雲端備份（`main.py` 背景執行緒）。
- 改為**純事件驅動**:每一次資料異動（入住、退房、延住、預約、付款、成本、房態、月租、維修、使用者、假日）都會**立即**觸發雲端自動備份，雲端永遠是最新狀態，不需等計時器。

### 附註
- 承 v3.9.13:202 房那筆的金額若仍顯示 NT$1,200，是因為先前的資料更正是在 App 執行中直接改 DB、被 App 記憶體內的舊值蓋回。需在 App 關閉狀態下更正才會生效（見交付說明）。

---

## v3.9.13 `2026-09-06` — 「修改預約」改為與「新增預約」同款介面，並帶入原折扣/天數

### 背景（承 v3.9.12）
- 202 房「8 天算 1 天」的**真正成因**:舊「修改預約」是簡化表單 —— **沒有天數欄位**(1.5 天無法自動計算)、**沒有折扣欄位**,且**未帶入當初預約輸入的折扣**。延長天數時金額算不出來、折扣又遺失,才造成大幅少收。

### 修正:編輯表單與新增表單一致
- 「修改預約」改成**與「新增預約」相同的介面**:新增 **天數**、**原價小計**、**折扣金額**、**自動金額(可手動覆蓋)** 欄位,以及右側**衝突檢查**與**費率參考**。
- **完整帶入原預約資料**:開啟編輯時自動填入房號、旅客、電話、入住/退房、方案、房價類型、**天數(由期間推算)**、**折扣金額**、金額。
- 依實際天數用**區塊封頂計價引擎**逐日(假日/平日)計價;改**入住時間 / 方案 / 天數 / 折扣**任一項都會自動重算(金額 = 期間總價 − 折扣);手動改金額則暫停自動計算。
- 編輯既有訂單允許**入住時間為過去**(不再誤擋);衝突檢查會**排除訂單自己**。
- 後端 `edit_booking` 現在會**一併儲存折扣**(舊版只存金額、不動折扣)。

### 實測
- 開啟一筆「1 天 + 折扣 400(金額 600)」訂單 → 表單正確帶入 天數1/折扣400/金額600/原價1000。
- 天數改 8 → 金額自動變 **8,400**(原價 8,800 − 折扣 400,折扣有被帶入並套用);折扣改 0 → 8,800;手動輸入 5,000 不被覆蓋。
- 按「儲存變更」→ 正確寫回 `amount=8,400, discount=400`。

---

## v3.9.12 `2026-09-06` — 修正「修改預約」多日計價錯誤（延長天數後金額未重算）

### 問題
- 房間 202 入住 8 天卻只收 1 天房費（NT$1,200）。排查後確認**計價引擎與「新增預約」皆正確**（8 天 = NT$8,800），錯誤出在**「修改預約」頁**。

### 根因（`booking_edit.html`）
1. 修改「退房時間」把訂單從 1 天延長為多天時，只更新了記憶的天數，**沒有重算金額** → 金額停在原本的 1 天價。
2. 即使有觸發重算，舊的 `editUpdateAmount()` 只查**單一時段**費率表（`EDIT_RATES[rt][pl]`），**忽略天數與跨日假日/平日**，多日訂單一律算成 1 個時段。

### 修正
- 「修改預約」改用與「新增預約」相同的**區塊封頂計價引擎**（`/api/booking-quote`），依訂單實際天數逐日（假日/平日）計價；金額 = 期間總價 − 既有折扣。
- 修改**入住時間 / 方案 / 退房時間**任一項都會重新計價；手動改過金額則暫停自動重算，直到下次結構性變更（與新增預約行為一致）。
- **實測**：把一筆 1 天訂單（NT$1,000）退房時間延長為 8 天 → 金額自動重算為 **NT$8,800**，按「儲存變更」正確寫回；手動輸入 5,000 不被覆蓋，改天數後恢復自動計價。

> 註：房間 202 目前這筆為**已入住**狀態，本次修正防止日後再發生；既有那筆的實收金額需另行更正（見交付說明）。

---

## v3.9.11 `2026-09-04` — 成本紀錄月份篩選修正、統計報表補上成本損益（限 admin）

### 1. 成本紀錄：修正月份篩選無效
- **根因**：篩選表單內有一個隱藏欄位 `year_month`（值固定為當月），與月份下拉選單同名，送出時覆蓋了下拉選的月份，導致永遠只顯示當月。
- **修正**：移除該重複隱藏欄位。月份下拉維持 `onchange` **即時**送出（選了就立即重新載入該月資料，不需確認按鈕）。
- **一併移除**：使用者反映「重設」按鈕幾乎用不到 → 移除。
- **實測**：切到 2026-08 立即載入上月資料（Rent 8,000／Laundry 450）；切回 2026-09 顯示當月（Utilities 1,200／Cleaning 300）。

### 2. 統計報表：補上「成本與損益」區塊（僅 admin 可見）
- 報表頁新增卡片：**期間營收、期間成本（含筆數）、淨利（營收−成本）、成本率、各分類成本明細**，並附前往成本紀錄的連結。
- **權限控管**：成本／損益數字僅在 `admin` 權限時計算並輸出到頁面；`manager` 等其他角色頁面上**完全不會出現**這些數字（後端就不傳，非僅前端隱藏）。
- **實測**：admin 看得到卡片（成本 1,500、淨利 −1,500、分類明細）；manager 進同一頁看不到任何成本區塊。

### 3. 退房超時自動延住費：確認功能正常（未消失）
- 針對回報「上次更新後超時自動計費似乎消失」進行完整排查：後端、API、前端彈窗三層皆正常。
- **實測**：對超過退房時間的在住房（延遲約 48 小時）退房，`/api/overdue-check` 回傳 `overdue=true, fee=2100`；前端確認退房時**正確彈出**「已超過退房時間，將產生 NT$ 2,100 的延住費用，確認嗎？」；實際退房寫入 `base=1,000 + 延住 2,100 = 3,100`。功能無異常，未做非必要更動。

---

## v3.9.10 `2026-08-31` — 修正:付款快捷鈕未自動開啟「新增付款」視窗

- **根因**：付款頁的深連結 JS 位於 `content` 區塊(base.html 於載入 bootstrap **之前**渲染),執行當下 `window.bootstrap` 尚未定義,`new bootstrap.Modal().show()` 被守衛擋掉而靜默不開窗。
- **修正**：改在 `DOMContentLoaded`(bootstrap 已載入)後執行,並加後備「觸發宣告式開窗鈕」。
- **真實瀏覽器實測**：按房間卡片「付款」→ 付款頁**自動開啟**新增付款視窗,並帶入房號 202 / 旅客 Michael Solomon（對應在住房客,連下拉也自動選定）。

---

## v3.9.9 `2026-08-31` — 房間總覽新增「付款」快捷鈕

### 新增：房間卡片一鍵付款
- 房間總覽中,使用中/即將退房房間的「延住」與「退房」按鈕之間,新增 **💵 付款** 按鈕。
- 按下直接跳到「付款管理」並**自動開啟「新增付款」視窗**,帶入該房號、旅客姓名(可對應到在住房客,連 booking 也一併帶入)。
- 免再換頁、手動輸入 — 客人臨時想付款或延住補款可直接處理。

---

## v3.9.8 `2026-08-25` — 修正雲端更新未覆蓋相同時間戳檔案

### 修正：雲端更新顯示成功但版本仍未更新
- 更新套用腳本的 `robocopy` 加入 `/IS`，即使來源與目的檔案大小/時間戳相同，也會確實覆蓋新版程式。
- 避免 `VERSION` 或同大小程式檔被 robocopy 略過，造成更新後仍停留在舊版本。

---

## v3.9.7 `2026-08-25` — 修正新增預約房間選單殘留過期預約時間

### 修正：房間選單顯示已過期或已不存在的下一筆預約
- 新增預約頁不再直接信任 `Room.next_booking` 的舊快取值。
- 改由目前時間即時查詢該房間「非取消狀態且入住時間尚未到」的最早預約。
- 沒有有效未來預約時，房間選單不再顯示過期時間。
- 新增回歸測試，涵蓋過期預約、殘留快取與有效未來預約。

---

## v3.9.6 `2026-08-15` — 修正:閒置/重啟後被登出(session 現在持久)

### 修正:一段時間沒用或重開程式後需要重新登入
- **根因 1（主因）**:session 簽章金鑰 `_SECRET` 原本存在記憶體、**每次伺服器重啟就重新產生**,一換金鑰所有登入立即失效。桌面版(v3.9.0)是「關視窗＝關伺服器」,所以用完關掉、稍後再開＝伺服器重啟＝被登出。以前瀏覽器版伺服器整天不關,故沒發生。（此邏輯自 v3.6.2 就存在,非 v3.9.5 引入。）
- **根因 2**:session 固定 12 小時到期,閒置超過即失效。
- **修正**:
  1. `_SECRET` 改為**持久化到 `session.key`**(安裝資料夾;git 忽略、雲端備份排除、自動更新覆蓋時保留)——重啟 / 自動更新後 session 依然有效。
  2. `SESSION_TTL_HOURS` 12 小時 → **1 年**,閒置再久也不會逾期。
- 實測:登入後重啟伺服器,同一 session 仍直接進入系統、不再跳登入。

> 註:此為單機、實體管控的櫃檯系統,依你需求刻意讓登入「放再久也不登出」。若日後換成多人共用或需自動登出,再調整 `SESSION_TTL_HOURS` 即可。

---

## v3.9.5 `2026-08-15` — 全資料變動自動備份 / 付款可選在住房客

### 新增：所有資料變動一律自動雲端備份（補齊先前未配線的端點）
之前僅入住 / 退房 / 預約(新增·修改·多筆·no-show 取消) / 延住 / 付款新增 / 清潔 / 維修更新 / 月租(建立·退租·續租) 會自動備份。本版補齊以下**同樣會改動營運或金錢資料、卻未觸發備份**的操作：
- **金錢相關**：成本 `costs` 的**新增 / 編輯 / 刪除**、付款**刪除**、**日結（關閉收銀）**。
- **預約**：**取消預約**（`/bookings/{id}/cancel`，先前只有 no-show 取消有備份）。
- **房間 / 住宿**：房態更新（`/room-management/update`）、**換房**（`/room-management/transfer`）。
- **維修 / 物業**：維修排程**建立 / 刪除**、物業建立。
- 備份沿用既有 `auto_backup()`：僅在已設定雲端且開啟自動備份時觸發，失敗絕不中斷主流程。

### 新增：付款管理「新增付款」可直接選擇在住房客
- 新增付款視窗頂端新增 **「選擇入住房客」下拉選單**，列出目前所有在住房客（房號 · 姓名）。
- 選取後**自動帶入** 房客姓名、房號，並連結其 `booking_id`（利於押金 / 收款歸戶）。
- 保留例外情境：下拉可選「手動輸入」，房客 / 房號欄位仍可自由編輯或手打。
- 新增 i18n 鍵 `payment.select_guest` / `payment.select_guest_hint` / `payment.manual_input`（中英）。

### 新增：使用者帳號管理 與 假日設定 也納入自動備份
- **使用者帳號**：建立 / 啟用停用 / 改密碼 / 編輯 / 舊版更新資料 五個端點皆觸發備份（`User` 本就在備份範圍內，故還原有效）。
- **假日設定**：新增 / 刪除 / 重新同步 皆觸發備份。
- **重要**：`HolidayCache`（含手動新增假日）先前**未**納入備份匯出/匯入，本版一併補上——否則備份假日變動卻無法還原。匯入採「有 key 才還原」保護，舊備份不會洗掉現有假日；開機仍會自動重抓 API 假日。

### 技術
- 事件端點備份配線：`routers/costs.py`、`routers/bookings.py`、`routers/rooms.py`、`routers/phase4.py`、`routers/admin.py`、`routers/auth.py`。
- `routers/phase4.py` `payments_page` 傳入 `active_stays`（`get_all_active_stays`）；`templates/payments.html` 加入下拉與自動帶入 JS。
- `services/backup.py`：`_export_db` / `_import_data` 新增 `holiday_cache`（Integer PK，pop_id）。
- 驗證：`compileall` 無語法錯誤；FastAPI app 匯入成功（83 routes）；`holiday_cache` 匯出/還原往復測試通過；zh/en JSON 解析通過。

---

## v3.9.4 `2026-08-03` — 程式內一鍵更新（更新前自動雲端備份）

### 新增：系統更新按鈕（雲端備份頁，僅 admin）
- 「🔄 系統更新」卡片:**檢查更新**(比對 GitHub 最新版) + **立即更新**。
- 按下更新後自動流程:
  1. **強制先雲端備份(Dropbox)** — 備份失敗即中止,**完全不動程式碼**。
  2. 從 GitHub 下載新版 → 解壓 → 以外部批次 `robocopy /E` 覆蓋程式(**無 `/MIR`,絕不刪資料**;明確排除 `bini_blooms.db` / `*.log` / 本機備份 / `.git`)。
  3. 重裝相依 → **自動重啟**桌面程式。
- 不必再重新下載 zip 手動安裝。

### 技術
- 新增 `VERSION` 檔為**單一版本來源**;`main.py` 讀取它,更新檢查亦比對它。
- 新增 `app/services/updater.py`(檢查 / 下載 / 產生更新批次)與 `/api/update/check`、`/api/update/run` 端點。
- 更新批次安全性、版本比較、解譯器解析、下載解壓皆通過測試。

> 注意:更新來源為公開的 GitHub repo(需將 repo 設為 Public)。首次要用此按鈕,需先手動安裝一次 v3.9.4;之後即可一鍵更新至 v3.9.5+。

---

## v3.9.3 `2026-08-02` — 月租收入認列改以「實際收款日」為準

### 變更：月租租金收入的認列基準 起租日 → 實際收款日（重要，會計口徑）
- **問題**：報表把月租租金認列在「起租日 (`start_date`) 所在月份」。若某期租金實際是在上個月收的、但這期的起租日落在本月，就會在本月報表被算進收入（使用者觀察到 206/207 在 8 月報表出現本不該計入的租金）。
- **修正**：月租租金改以 **`MonthlyRental.created_at`（該筆月租/續租建立＝當下收租金的時刻）** 認列——租金在「實際收取」的那個月才計入收入。新增 / 續租都在收款當下才認列，完全符合「續約（收款）後才算收入」。
- 影響範圍：統計報表「各房間出租統計 / 月租收入 / 訂單數」與成本模組「損益(P&L) 月租收入」皆改用同一個 `recognition_date()` helper,口徑一致。
- **注意**：這是會計口徑變更——過去「起租日月份 ≠ 建立月份」的月租,其收入會從起租月移動到收款月(通常兩者相同,少數手動改過起租日的會位移)。

### 稽核：其餘金額計算已驗證正確（本次一併檢查全部會計邏輯）
- 月租每期租金只認列一次(不逐月重複)；免費取消(15分內)不計營收；換房舊房不重複；押金不被加進營收；入住租金 = 房租+延住+加收。
- 服務層 16 項 + 實機報表頁端到端驗證全通過。

---

## v3.9.2 `2026-07-05` — 免費取消收款修正 / 修改預約自動帶出 / 月租續租 / 下一筆預約唯讀

### 修正：免費取消（15 分內退房）確認視窗仍要求收款
- **根因**：頁面主體已依免費取消歸零顯示，但「請收取餘額」確認視窗的 JS 用的是原始 `balance_due`（未考慮免費取消）。
- **修正**：點擊當下若仍在免費取消窗內（`isFreeCancelNow()`，隨倒數即時判斷），跳過收款視窗直接完成退房；超時檢查同樣跳過。窗內開頁、窗外點擊的邊界情況以點擊當下時間為準。

### 修正：修改預約未自動帶出退房時間
- 與新增預約一致：變更入住時間或方案時，**退房時間自動重算**。
- 沿用既有預約的期數（多日預約改期不會被誤縮成一天）；人工調整退房時間則更新記住的期數。

### 新增：月租套房「續租一個月」
- 房間管理月租卡片新增 **🔁 續租一個月** 按鈕（含確認視窗，顯示新租期與將記錄的租金）。
- 實作為「一期一列」：目前列標記 `renewed`、建立新一期（起=原迄、迄=+1 月），歷史可稽核，且**續租月租金正確計入報表營收**（報表以 start_date 歸屬）。
- 押金結轉不重複收取；續租租金記錄至付款記錄；最終退租時退還結轉押金。

### 變更：房間管理「下一筆預約」改為唯讀
- 由系統即時計算（與房間總覽同邏輯），人員無法手動修改；儲存房態不會洗掉系統值。

---

## v3.9.1 `2026-07-05` — 預設語言改為英文（含登入頁）

### 變更：系統預設語言 中文 → 英文
- 操作人員為外籍人士：**首次開啟（無語言 cookie）一律顯示英文**，包含登入頁。
- 語言判斷唯一入口 `get_lang()` 預設值 `zh` → `en`；en/zh 語言檔 641 鍵完全同步，無殘留中文。
- 選擇會記在 cookie（一年），切過中文的使用者不受影響。

### 新增：登入頁語言切換按鈕（EN / 中文）
- 登入頁右上角新增切換按鈕（原本只有登入後的主介面才有）。
- `/set-lang` 本就位於免登入白名單，登入前即可切換。

---

## v3.9.0 `2026-07-04` — 桌面單一視窗模式（專屬圖示、一次一視窗）

### 新增：桌面應用程式（不再經過瀏覽器）
- 以 **pywebview** 內嵌視窗開啟系統，具**專屬圖示**（視窗、工作列、桌面捷徑），操作員不會再看到瀏覽器分頁/網址列。
- 新增啟動器 `desktop.py`：於同進程啟動 uvicorn → 開單一視窗 → **關視窗即完整收工**（伺服器與背景備份一併停止）。
- 埠優先 8000，被占用時自動改用空閒埠。

### 修正：一次只能開一個視窗（根治備份錯亂 / 輸入未存）
- **根因**：操作員找不到既有視窗又開一個 → 同機出現兩個實例（各自資料庫 / 各自備份排程）→ 備份互相覆蓋、輸入存到另一份。
- **做法**：以 Windows 具名 mutex 強制**全機單一實例**；再次啟動時**把既有視窗叫到最前面並立即結束**，不會開出第二個視窗（`start.bat` 亦然）。

### 安裝 / 打包
- `install.bat`：複製 `desktop.py` / `icon.ico`，並於桌面建立帶圖示的 **BINI Blooms PMS** 捷徑。
- `start.bat`：改為以 `pythonw` 無主控台啟動 `desktop.py`；相依偵測納入 `pywebview`。
- `requirements.txt`：新增 `pywebview==6.2.1`、`pythonnet==3.1.0`（Windows 用 WebView2 後端）。
- 版本統一 v3.9.0（app / installer / 啟動器 / README / FastAPI metadata）。
- 圖示採用**官方 BINI logo 房屋圖**（自 `Transient icon.png` 去背裁切產生多尺寸 `icon.ico`）。
- 修正：`desktop.py` 強制 stdout/stderr 為 UTF-8——避免非 UTF-8 主控台（cp950）下，啟動訊息的 emoji 造成伺服器啟動失敗。

> 註：本版仍需目標電腦安裝 Python 3.12(同前)；日後若要「免裝 Python 的單一 .exe」再以 PyInstaller 打包即可。

---

## v3.8.3 `2026-07-04` — 衝突偵測修正 / 甘特圖改為 2 週

### 修正：入住與預約的時段衝突未被阻擋（重要）
- **根因 1**：`get_conflict_detail()` 只比對 `Booking`，完全未比對 `ActiveStay`（目前入住），所以建立預約時若該房有人正在入住且時段重疊，查不到而放行。
- **根因 2**：入住登記流程（`checkin.py`）**從未呼叫任何預約衝突檢查**（僅檢查日期格式與維修排程），因此入住可蓋過既有預約而不被阻擋。
- **修正**：`get_conflict_detail()` 現在同時比對「既有預約 + 目前入住中旅客」（新增 `include_stays` 參數）；入住登記新增預約衝突檢查（排除旅客自己的來源預約）。兩個方向（入住蓋預約、預約蓋入住）皆會阻擋。
- 相鄰不重疊（如退房 22:00 與預約 22:00）不誤判；不同房間、旅客憑自己預約入住不誤判。

### 變更：甘特圖顯示範圍改為 2 週
- 顯示範圍由「今天起 7 天」改為「今天起 **2 週（14 天）**」。
- 維持原本 36px/小時的細緻顯示（每天含 4 小時刻度格線），自動捲動到現在時刻。

### 版本一致性
- CMD 標題列、啟動橫幅、安裝程式、README、FastAPI metadata 統一為 v3.8.3。

---

## v3.8.2 `2026-06-21` — 付款歸屬修正：以入住建檔時間為界

### 修正：入住時間被設為未來時，入住押金在總覽顯示 0
- **根因**：付款歸屬以可被櫃檯編輯的 `checkin_time` 當下界；若入住時間設在押金收取之後，押金被排除（顯示「已收款 = 0」）。
- `ActiveStay` 新增 `created_at`（入住實際建檔時間，含自動遷移），作為付款歸屬下界，不再受 `checkin_time` 前後調整影響。
- `get_paid_for_stay()` / `get_deposit_paid()` / 免費取消退款 三處統一改用 `_stay_payment_floor()`（建檔時間，舊資料回退 `checkin_time`），避免三處邏輯再次分歧。

### 版本一致性
- CMD 標題列、啟動橫幅、安裝程式、網頁頁尾、FastAPI metadata 統一為 v3.8.2。

---

## v3.8.1 `2026-06-19` — 付款系統修正（押金顯示 / 免費取消退款）

### 修正：預約押金入住後未顯示於房間總覽
- **根因**：押金以 `created_at >= checkin_time` 過濾，預約階段收取的押金建立時間早於入住時間而被排除，房間總覽顯示「已收款 = 0」。
- `ActiveStay` 新增 `booking_id` 欄位（含自動遷移），入住時記錄關聯預約編號。
- `get_paid_for_stay()` / `get_deposit_paid()` 改為納入「入住後收款 **或** 關聯預約的押金」兩種來源。
- **補強**：預約（單筆與多段）建立押金時，將預約編號寫入 `Payment.booking_id`；否則上述關聯比對永遠配不到（多段以首筆預約為關聯）。

### 修正：免費取消自動退還押金
- 入住 15 分鐘內退房（免費取消）時，自動為該次入住的押金建立退款記錄（`is_refund`），避免押金留在系統中造成營收灌水。
- **補強**：退款查詢與 `get_deposit_paid()` 採相同範圍（入住後收款 **或** 關聯預約的押金），確保預約階段收的押金也會被退，兩處邏輯一致。
- 結帳頁顯示「押金已自動退還」提示；免費取消隱藏收款欄位，帳單總額顯示 NT$ 0。

### 版本一致性
- CMD 標題列、啟動橫幅、安裝程式、網頁頁尾、FastAPI metadata 統一為 v3.8.1。

---

## v3.8.0 `2026-06-19` — 預約押金功能 / 付款即時備份

### 新增：預約押金功能
- 新增預約時可設定**押金金額**與**付款方式**（現金/轉帳/信用卡/其他）
- 多段預約（Add Slot）也支援押金
- 押金記錄為 `is_deposit=True`，不計入營收（P&L 只使用 StayLog）
- 入住登記（Check-in）同時支援帶入押金
- 退房頁面顯示「已收押金」金額，餘額計算正確扣除押金

### 移除：預約自動取消（No-show）機制
- 取消超過入住時間 30 分鐘自動刪除預約的功能，避免已付訂金客人晚到被刪除
- 預約列表提示改為「預約保留至入住當日，無自動取消機制」
- 保留 15 分鐘預約提醒（橘色橫幅），但僅提醒不自動取消
- 保留手動取消按鈕（保留預約/取消預約），供櫃檯自行判斷

### 修正：付款即時觸發雲端備份
- `create_payment()` 建立任何付款（押金/收款/退款）後立即觸發 `auto_backup`
- 確保押金記錄**不會遺漏**，不再依賴 30 分鐘定時備份
- 獨立付款頁面 `/payments/add` 新增的款項也立即備份

### 版本一致性
- CMD 標題列、啟動橫幅、安裝程式、網頁頁尾、FastAPI metadata 統一為 v3.8.0

---

## v3.7.4 `2026-06-07` — 入住登記支援多日（同新增預約設計）

- 入住登記新增「**天數**」欄位:走進來要住多日的客人,直接填天數即可
- **退房時間唯讀(反灰)**,由系統依「入住時間 + 天數 × 方案」自動帶出
- 房租**自動計算**(同一套計費引擎,12hr × N 正確套用 24hr 區段上限),並新增「**折扣金額**」欄位;基本房租 = 原價小計 − 折扣,仍可手動微調
- 折扣存入入住資料(`active_stays.discount`),備份/還原一併涵蓋
- 從預約帶入時,自動推算天數並沿用預約已談定的金額

---

## v3.7.3 `2026-06-07` — 新增預約防呆：退房時間唯讀

- 新增預約的「退房時間」改為**唯讀(反灰)**,操作人員無法手動更改
- 一律由系統依「入住時間 + 天數 × 方案時數」**自動帶出**,避免人為輸入錯誤

---

## v3.7.2 `2026-06-07` — 多日預約計費修正（12hr 區段上限）

### 修正
- 多日預約金額改用**計費引擎**計算,正確套用「每 12 小時區段上限」:
  - 連續兩個 12hr 會套 **24hr 房價上限**,不再 800+800=1600
  - 例:12hr × 5(=2.5 天,週間)= **NT$2,800**(修正前誤算為 4,000)
  - 24hr × N、跨假日皆逐區段依當日費率正確加總
- 新增 `/api/booking-quote` 由後端引擎報價;預約金額與實際入住/退房計費一致
- 金額仍可手動調整(手動輸入後即停用自動計算,直到變更日期/方案/天數才恢復)

---

## v3.7.1 `2026-06-07` — 多日預約（天數＋折扣自動計費）

### 新增預約改良
- 新增「**天數**」欄位：連續多日預約只要填天數,**自動算出退房時間**(入住＋天數×方案時數),不必再逐段 Add Slot
- 金額**自動計算**:逐日判斷當天假日/非假日,各自套用房價加總(原價小計)
- 新增「**折扣金額**」欄位(常用於連續天數優惠);最終金額 = 原價小計 − 折扣,且仍可手動微調
- 折扣**單獨存進預約資料**(`discount` 欄位),日後可追;金額欄位顯示折扣後的最終價
- 保留原本的「新增時段(Add Slot)」供不同房間/不連續日期使用

---

## v3.7.0 `2026-06-07` — 月租套房 / 計費修正 / 雙重雲端備份

### 新功能：月租套房（月租方案）
- 房間管理可「設定月租」：承租人、電話、起租日（自動算一個月）、月租金、押金、收款方式
- 新增房態 **月租套房**；月租期間此房**無法入住、預約、延住、換房**
- 月租「辦理退房」會提示**是否退還押金**並可輸入退款金額
- 租金列入**統計報表**營收與每房統計；月租也顯示於**甘特圖**

### 計費修正（重要）
- **延住費改以「入住時間」為基準計算**：同日 12hr→24hr 延住只收方案差價（例：假日 +NT$200），不再誤收隔日 12hr 上限（NT$800）
- 多次分段延住不再重複超收；單次與分次延住金額一致
- **房間總覽／退房頁的「餘額應收」改為扣除所有已收款**（押金＋一般付款），付款管理新增的款項會正確反映

### 備份強化
- 修正**月租資料未被備份**的問題（匯出／還原皆已納入）
- 新增**每 30 分鐘定時雲端備份**，使用**獨立檔名**與獨立本機資料夾，與事件備份互不覆蓋（雙重保障）

### 啟動／安裝修正
- `start.bat` / `install.bat` 自動偵測「真正可用的 Python」（避開 Windows 市集替身）、強制 UTF-8、改為 CRLF 純 ASCII，解決啟動亂碼與錯誤

---

## v3.6.2 `2026-05-21` — 假日管理改為月曆顯示

### 新設計：假日管理月曆介面
- 全年 12 個月月曆一次顯示，以**顏色區分**假日/週末/今日
- **粉紅底** = 國定假日（自動抓取）
- **黃色虛線框** = 手動新增假日
- **淡粉色** = 週末
- **深粉框** = 今日
- 假日格子顯示節日名稱（如「端午節」），**hover 顯示詳細資訊**（來源、說明）
- **點擊假日格子** → 跳出確認後刪除
- **點擊非假日格子** → 自動填入日期，展開新增面板
- 月份快速跳轉 chip（1月~12月）
- 重新抓取 + 新增按鈕保留

---


## v3.6.1 `2026-05-21` — 假日管理入口 / 入住登記有效預約修正

### Bug 修正：假日管理找不到入口
- `holidays` 未加入 `ALL_NAV_PAGES`、`ROLE_PERMISSIONS`、`ROLE_NAV` 及 `base.html`
- 修正：假日管理現在出現在導覽列（Admin + Manager 可見）

### Bug 修正：入住登記右側顯示過期/已入住預約
- `checkin.py` 的 bookings filter 只排除 `已取消/No-show`，未排除 `已入住`
- 修正：改用與預約管理相同的 `CANCELLED_STATUSES`（已取消、No-show、已入住）
- 同時排除超過 35 分鐘未入住的過期預約（這些已被系統標記為 No-show）

---


## v3.6.0 `2026-05-20` — 版本升級

### 版本升級 v3.5.20 → v3.6.0
- 整合自動政府假日抓取（v3.5.20）為正式 v3.6.0 里程碑版本
- 同步修正 FastAPI 內部 version 欄位（舊版殘留 3.5.18，現統一為 3.6.0）
- 所有版本顯示位置（登入頁、頁尾、install.bat、start.bat）均已確認一致

---


## v3.5.20 `2026-05-20` — 自動抓取政府假日 / 假日管理介面

### 新功能：啟動時自動抓取政府假日
- 每次啟動程式時自動從政府開放資料 API 抓取當年及次年假日
- API 來源：data.ntpc.gov.tw + jsdelivr.net/gh/ruyut/TaiwanCalendar
- 若 API 無法連線（離線）→ 自動使用內建備份清單（fallback）
- 抓取結果存入 `holiday_cache` 資料表，無需每年手動更新程式碼

### 新功能：假日管理介面 `/admin/holidays`
- 檢視當年/歷年假日清單（來源標示：政府API / 內建 / 手動）
- 手動新增假日（不會被自動抓取覆蓋）
- 刪除不正確的假日
- 「重新抓取」按鈕：清除並重新從 API 取得本年假日

### 費率計算優先使用 DB 假日快取
- `is_holiday()` 優先查詢 `holiday_cache` 資料表
- 若 DB 無此年資料則退回靜態清單
- 補班日若逢六日仍計為假日費率

---


## v3.5.19 `2026-05-20` — 2026假日清單修正

### Bug 修正：2026年端午節日期錯誤
- **錯誤**：`2026-05-20`（非假日，此日為週三）被誤標為端午節
- **正確**：`2026-06-19`（農曆五月初五，真正的端午節）
- 導致 5/20 Check-in 費率被誤判為假日費率（NT$1,200）→ 應為平日費率（NT$1,000）

### 同步新增 2026 年其他新增假日（依行政院人事行政總處公告）
- `2026-02-15` 小年夜（自2026年起新增國定假日）
- `2026-09-28` 孔子誕辰紀念日/教師節（自2025年起新增）
- `2026-10-26` 台灣光復節補假（10/25逢日順延）
- `2026-12-25` 行憲紀念日（自2025年起新增）

### 補假日修正
- `2026-02-27` 和平紀念日補假（2/28逢六）
- `2026-10-09` 國慶日補假（10/10逢六）

---


## v3.5.18 `2026-05-18` — Check-in 時間 T 符號修正 / Console 錯誤修正

### Bug 修正：退房時間顯示 T
- **原因**：`autoCheckout()` 產生的格式是 `YYYY-MM-DDT HH:MM`（HTML datetime-local 格式）
  但 flatpickr 需要 `YYYY-MM-DD HH:MM`（空格）
- **修正**：改用空格分隔，並同步更新 flatpickr 實例

### Bug 修正：Console JSON.parse SyntaxError
- **原因**：假日日期 JSON 放在 HTML attribute `value=""` 中，`"` 被跳脫為 `&quot;`
  導致 `JSON.parse(hwInput.value)` 失敗
- **修正**：改用 `<script type="application/json">` 標籤，讀取 `textContent` 不受 HTML 跳脫影響

### 說明：5/20 費率 NT$1200 是正確的
- `2026-05-20` = 端午節（在 TW_NATIONAL_HOLIDAYS 清單中），屬假日費率
- 24hrs 假日 = NT$1,200 完全正確，非 Bug

---


## v3.5.17 `2026-05-16` — Logo 更換 / 版本全號顯示 / Check-in 預設時間

### 外觀：LOGO 更換
- 登入頁、導覽列、Prototype Hub 首頁的 🌸 花朵 emoji 全部改為正式 BINI Blooms LOGO 圖片
- 圖片路徑：`/static/bini_blooms_logo.jpg`

### 版本號：顯示完整版本
- 頁尾、登入頁硬寫的 `v2.2`、`v3.5` 改為動態讀取 `APP_VERSION`
- 現在顯示完整版本號如 `v3.5.17`，方便確認是否為最新版

### Check-in：預設現在時間
- 入住登記頁面「入住時間」欄位預設帶入目前時間（不再空白）
- 保持可調整，但不允許設為過去時間（`min=now` 限制）
- flatpickr + HTML min 雙重防護，每分鐘更新最小時間

---


## v3.5.14 `2026-05-10` — 備份修正：包含 Payment/押金資料

### Bug 修正：雲端備份未備份 Payment 表（包含押金記錄）
- **問題**：`_export_db()` 只備份 5 張表，Payment 表（含押金 is_deposit=1）完全未備份
- **修正**：備份現在包含所有 12 張資料表：
  - ✅ payments（含押金、退款記錄）
  - ✅ cashier_sessions
  - ✅ cost_entries
  - ✅ users
  - ✅ properties
  - ✅ maintenance_schedules
  - ✅ activity_logs
- 換電腦後還原可完整恢復所有押金與付款記錄

---


## v3.5.13 `2026-05-10` — 精準營收計算

### 改進：統計報表 & 成本紀錄 — 精準營收公式
- **舊版**：計入未入住的預約金額（預約了但未到，或仍在入住中）
- **新版**：只計算真實已收到的金額：
  - ✅ `StayLog.total_charged`：已完成退房的住宿費（含延住費）
  - ✅ `Payment.is_deposit=1`：已收押金
  - ❌ 預約金額（未入住前不計入）
  - ❌ 入住中的 total_due（未退房前不計入）
- 兩個頁面使用相同公式，數字完全一致

---


## v3.5.12 `2026-05-10` — 成本紀錄與統計報表營收一致（根本原因修正）

### Bug 根本原因：CANCELLED 集合定義不同
- **統計報表** 排除：`{已取消, No-show}`（「已入住」仍計入）
- **成本紀錄** 排除：`{已取消, No-show, 已入住}`（「已入住」被錯誤排除）
- 因此所有已辦理入住的預約金額在成本紀錄中消失，造成數字偏低
- **修正**：成本紀錄改用與統計報表相同的集合 `{已取消, No-show}`
- 兩個頁面現在使用完全相同的邏輯，數字應一致

---


## v3.5.11 `2026-05-10` — 成本紀錄與統計報表說明

### 說明澄清：兩頁面日期範圍不同，非 Bug
- **統計報表**：有自訂日期範圍（可選任意期間，如 5/1-5/10）
- **成本紀錄**：月度 P&L，永遠計算**整個月份**（如全部 2026-05）
- 兩者本來就不同，不是計算錯誤
- 成本紀錄的「本月營收」KPI 卡片加上說明：「整月合計（StayLog＋預約）」

---


## v3.5.10 `2026-05-10` — 成本紀錄與統計報表營收一致

### Bug 修正：兩個頁面營收數字不同
- **原因**：成本紀錄多計算了 ActiveStay（目前入住中）的 total_due，
  但統計報表的 `range_revenue` 不含 active stays
- **修正**：成本紀錄改為完全對齊統計報表的 `range_revenue` 計算：
  - ✅ StayLog.total_charged（已退房完成）
  - ✅ Booking.amount（有效預約）
  - ❌ ActiveStay（移除，與統計報表一致）
- 兩個頁面現在顯示相同的月份營收數字

---


## v3.5.8 `2026-05-10` — 成本記錄 500 錯誤徹底修正

### Bug 修正：/costs 頁面持續 500 錯誤
- v3.5.7 的修正不完整，StayLog 和 Booking 仍使用 SQL 欄位過濾
- **徹底重寫**：`monthly_pnl()` 所有資料過濾改為 Python 端執行，完全不依賴 SQL WHERE 欄位比對
- 每一筆資料都加 try/except 防護，確保單筆異常不影響整體計算
- 三層 fallback：StayLog → Booking → Payment 表

---


## v3.5.7 `2026-05-10` — 成本記錄 Internal Server Error 修正

### Bug 修正：/costs 頁面 500 錯誤
- **原因**：v3.5.5 修改成本記錄營收計算時，對 ActiveStay 使用 SQL 欄位過濾
  (`ActiveStay.checkin_time >= date_from`)，在舊資料庫可能引發欄位不存在錯誤
- **修正**：改為 Python 端月份比對（`checkin_time[:7] == year_month`），
  避免 SQL 層欄位相容性問題，並加入 try/except 防護

---


## v3.5.6 `2026-05-10` — 統計報表圖表修正

### Bug 修正：統計報表三個圖表未顯示
- **原因**：Chart.js 從 CDN 載入，若 CDN 暫時無法連線或載入慢，圖表初始化時 `Chart` 物件尚未可用
- **修正**：
  - 加入 `unpkg.com` 備援 CDN（主 CDN 失敗自動切換）
  - 圖表初始化改為 `DOMContentLoaded` 後執行
  - 加入 `typeof Chart === "undefined"` 重試機制（每 500ms 重試直到載入完成）
  - `tojson` 加上 `safe` 過濾器，確保 JSON 不被 HTML 跳脫

---


## v3.5.5 `2026-05-10` — 成本紀錄營收計算修正

### Bug 修正：成本紀錄與統計報表營收不一致
- **問題**：成本紀錄用 Payment 表計算營收（實際收款），統計報表用 Booking + StayLog 計算（訂單金額）
- **修正**：成本紀錄改為與統計報表相同的計算來源：
  - StayLog（已退房完成的住宿，total_charged）
  - Booking（有效預約金額）
  - ActiveStay（目前入住中，total_due）
- 三者加總 = 統計報表顯示的營收，確保兩個頁面數字一致
- 若當月完全無記錄，退回使用 Payment 表作為 fallback

---


## v3.5.4 `2026-05-10` — 押金累加 Bug 修正

### Bug 修正：押金金額跨次入住累加
- **問題**：退房後重新入住，押金欄位會累加前次入住的押金記錄
- **原因**：`get_deposit_paid()` 只過濾 room_id，未限制時間範圍，導致查詢到所有歷史押金
- **修正**：加入 `created_at >= 當次入住時間` 的條件，只計算當次入住之後的押金記錄
- 影響範圍：房間總覽費用顯示、退房頁面押金/餘額計算

---


## v3.5.3 `2026-05-10` — 付款刪除 / 預約列表修正

### 付款管理 — Admin 刪除權限
- Admin 帳號在付款管理每筆記錄右側新增 🗑 刪除按鈕
- 刪除前跳出確認對話框，避免誤刪
- 刪除操作寫入 activity log
- 僅 Admin 角色可見，其他角色不顯示

### 預約管理 — 已入住排除
- 「已入住」與「即將到店」狀態的預約不再顯示在預約列表
- 統一使用 `CANCELLED_STATUSES` 集合（包含：已取消、No-show、已入住、即將到店）

---


## v3.5.2 `2026-05-09` — 費用顯示 / 退房收款提醒

### 房間總覽 — 使用中房間顯示費用
- 使用中的房間卡片新增費用摘要區塊：
  - 💰 **應付總額**（base_rent + extension_fee + extra_fee）
  - 💳 **已收押金**（Payment 表 is_deposit=1 加總）
  - **餘額應收**（應付總額 - 已收押金），餘額 > 0 顯示紅色，結清顯示綠色 ✅

### 退房頁面 — 費用摘要 + 收款確認
- 退房費用區塊新增押金與餘額顯示（同房間總覽格式）
- 退房時若有未結餘額，**自動彈出收款提醒視窗**：
  - 顯示應付總額、已收押金、餘額應收
  - 確認收款後才完成退房
  - 可取消退回修改
- 若餘額已全數結清（≤ 0），直接退房無需額外確認

### 退房流程（v3.5.2）
```
[確認退房] 按下
    ↓
超時檢查（若有超時 → 先確認延住費用）
    ↓
餘額確認（若有未收餘額 → 收款提醒視窗）
    ↓
確認退房完成
```

---


## v3.5.1 `2026-05-09` — 預約入住提醒 / 自動取消 No-show

### 新功能：預約入住前 15 分鐘提醒
- 入住時間前 15 分鐘，全站顯示**橘色警告橫幅**，含聲音提示
- 橫幅顯示房號、旅客姓名、剩餘分鐘數
- **直接在橫幅上操作**：
  - ✅ **保留預約** — 確認預約有效，橫幅消失
  - ❌ **取消預約** — 立即取消，標記為已取消，橫幅消失
- 每 60 秒自動輪詢，確保不遺漏

### 新功能：預約到期自動取消（No-show）
- 超過預約入住時間 **30 分鐘**未辦理入住 → 系統自動標記為 **No-show**
- 觸發時機：每次 `/api/booking-soon` 輪詢時執行（每 60 秒）
- No-show 的預約從預約列表消失（同已取消）

### 新功能：新增預約成功提醒
- 預約建立成功後，右下角彈出橘色提醒 Toast（15秒後自動消失）
- 提醒服務人員告知旅客：入住時間起 30 分鐘未入住將自動取消

### 預約管理頁面說明
- 頁面頂部新增政策說明：預約到達入住時間後 30 分鐘未入住，系統自動取消並標記 No-show

---


## v3.5.0 `2026-05-09` — 延住計費修正 / 超時退房確認 / 退房前提醒

### Bug 修正：延住計費邏輯

**問題：** 退房超時自動計算延住費用時，使用的是「每半小時 × 時薪」的簡單計算，完全忽略系統在「延住處理」分頁中設定的分段上限計費規則。

**修正：** 超時退房費用現在與「延住處理」使用完全相同的計費邏輯：
```
每 12 小時為一個計費區間
第 1 區間（0-12hr）：上限 12hrs 房價（平日 NT$800 / 假日 NT$1,000）
第 2 區間（12-24hr）：上限 24hrs-12hrs 差額（平日 NT$200 / 假日 NT$200）
時薪：NT$200/hr（分段計算，每段不超過各段上限）
```

### 新功能：超時退房確認對話框

當服務人員點「確認退房」時，若系統偵測到超時：
- 彈出確認視窗，清楚說明超時時間與系統計算費用
- 提供兩個選項：
  - **確認延住** — 套用系統計算的延住費用
  - **忘了退房** — 費用歸零，完成退房
- 可手動調整延住費用，避免系統判斷錯誤導致誤收費

### 新功能：退房前 15 分鐘提醒

- **全站通知**：無論在哪個分頁，退房時間前 15 分鐘自動顯示紅色警告橫幅
- **聲音提醒**：同步發出提示音
- **自動輪詢**：每 60 秒檢查一次，確保不漏接
- **可關閉**：點 ✕ 關閉，若下一次輪詢仍未退房會再次提示
- **內容顯示**：房號、旅客姓名、剩餘分鐘數、直接連結至房間總覽

---

## v3.4.4 `2026-05-05`
- 雲端備份最近紀錄固定高度 320px + 捲軸
- 登入後自動檢查雲端連線，異常時右下角顯示警告 Toast

## v3.4.3 `2026-05-04`
- 房間管理同步維修排程狀態（維修中）
- 甘特圖顯示手動設定維修中的房間
- 新增預約 + 入住登記加入維修排程衝突確認
- 預約管理表頭新增「預約編號」欄位對齊資料列

## v3.4.2 `2026-05-04`
- Dropbox OAuth2 長效認證（自動更新 Access Token）
- 完整備份：涵蓋所有 12 張資料表
- 換電腦提示與全新安裝偵測

## v3.4.1 `2026-05-03`
- 統計報表日期選取 + 快選按鈕功能修正（時區 Bug）
- 版本進版

## v3.4.0 `2026-05-02`
- 假日定義修正 / 多段預約 UI / 甘特圖 / 取消預約不顯示

## v3.3.x `2026-05-02`
- 多段預約排版、Slot 表單、JS Bug 修正

## v3.3.0 `2026-05-02`
- 多段預約、預約排序、預約修改、維修排程、備份觸發擴充

## v3.2.0 `2026-05-01`
- 安裝系統重構、路徑絕對化、假日邏輯修正

## v3.0.0 `2026-04-30`
- 正式版本：RBAC 登入、付款、備份、多館別、報表
