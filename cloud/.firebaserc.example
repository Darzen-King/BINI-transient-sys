{
  "_comment": [
    "Copy to .firebaserc and fill in your OWN project ids. .firebaserc is git-ignored.",
    "Two separate Firebase projects are mandatory: DEV and PROD.",
    "Test machines must only ever target the DEV alias."
  ],
  "projects": {
    "default": "REPLACE_WITH_DEV_PROJECT_ID",
    "dev": "REPLACE_WITH_DEV_PROJECT_ID",
    "prod": "REPLACE_WITH_PROD_PROJECT_ID"
  }
}
